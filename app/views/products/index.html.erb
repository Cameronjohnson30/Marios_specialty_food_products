<h1>Products</h1>

<% if is_admin? %>
<p><%= link_to "Add new product", new_product_path %></p>
<% end %>

<h4>All Products</h4>
<% if @products.any? %>
  <ul>
    <% @products.each do |product| %>
      <li><%= link_to product.name, product_path(product) %></li>
    <% end %>
  </ul>
<% else %>
  <p>There are no products yet please add some. </p>
<% end %>

